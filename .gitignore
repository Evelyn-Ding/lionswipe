# Real Supabase keys/config — never commit this. Copy config.example.js -> config.js locally
# and on your deploy target instead.
config.js

# OS/editor cruft
.DS_Store
*.swp

# Vercel
.vercel

# Node
node_modules/

# Local env vars for `vercel dev` / scraper testing (service role key, etc.)
.env
.env.local

# scripts/scrape-menus.js debug dumps (HTML/screenshots of the target page)
scripts/scrape-output/
.env*

# Hackathon demo account setup notes (contains a login password) — delete once done
DEMO_ACCOUNT_REVERT.md
seed_demo_account.sql
swipe_market_schema.sql
